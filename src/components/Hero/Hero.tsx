import React from 'react';

interface HeroProps {
    dict: {
        title: string;
        subtitle: string;
        placeholder: string;
        button: string;
    };
}

export default function Hero({ dict }: HeroProps) {
    return (
        <section>
           
        </section>
    );
}